<Page x:Class="AmazingApp.Views.HomeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AmazingApp.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HomeView"
      DataContext="{StaticResource sharedViewModel}">

    <Grid>
        <Label Background="Transparent" Foreground="#FFFFFF" Content="You are now witnessing Jesper Andersen amazing GUI" Margin="10,10,0,0" Height="30" VerticalAlignment="Top"/>
        <Label Background="Transparent" Foreground="#FFFFFF" Content="In here you can Add end Update customers in the Database without the need to contact IT :)" 
               Margin="10,50,0,0" Height="30" VerticalAlignment="Top"/>
    </Grid>
</Page>
